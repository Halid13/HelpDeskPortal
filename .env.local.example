# Configuration base de données - copier les mêmes valeurs que MasterMonitor
DATABASE_URL=postgresql://postgres:Azerty1234%40@localhost:5432/mastermonitor

# URL du MasterMonitor pour verifier l'email dans l'AD
MASTERMONITOR_URL=http://localhost:3000

# Optionnel : forcer un domaine email entreprise (ex: monitor.local)
# COMPANY_EMAIL_DOMAIN=monitor.local

# Optionnel : forcer SSL
# PGSSL=true
